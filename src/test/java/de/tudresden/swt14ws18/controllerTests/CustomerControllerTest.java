package de.tudresden.swt14ws18.controllerTests;

import de.tudresden.swt14ws18.AbstractWebIntegrationTests;

/**
 * Dieser Controller-Test ist NICHT FUNKTIONSFÄHIG!! Dies liegt an der Fehlfunktion des AbstractWebIntegrationTests!
 * 
 * Da diese Anforderung nur optional ist, wird die Lösung des Problems eher an das Ende der Testphase gelegt!
 * 
 * @author Reinhard_2
 *
 */

public abstract class CustomerControllerTest extends AbstractWebIntegrationTests {
    /*
     * @Autowired LotterieController controller;
     * 
     * @Test public void sampleMvcIntegrationTest() throws Exception {
     * 
     * mvc.perform(get("/")).andExpect(status().isOk());
     * 
     * }
     * 
     * @Test public void sampleControllerIntegrationTest() {
     * 
     * ModelMap model = new ExtendedModelMap();
     * 
     * String returnedView = controller.Toindex(model); //gameoverview(model); //profil(model);
     * 
     * assertThat(returnedView, is("index"));
     * 
     * //Iterable<Object> object = (Iterable<Object>) model.get("totoGameTypes"); // assertThat(object, is(notNullValue())); }
     */
}
